{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build", 
      "config": { "distDir": "dist" } 
    },
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "index.html"],
        "buildCommand": "npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.js" }
  ]
}
